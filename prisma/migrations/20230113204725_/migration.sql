BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[EmployeesOnPath] ADD [present] BIT CONSTRAINT [EmployeesOnPath_present_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
