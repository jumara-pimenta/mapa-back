BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Path] ALTER COLUMN [startedAt] DATETIMEOFFSET NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
